"""全局默认参数 —— 所有回测入口的默认值都从这里读取。

修改本文件即可统一调整：回测时间区间、初始资金、默认标的。
（佣金/印花税等交易规则参数见 server/config.py，不在本文件。）
"""

# 默认回测时间区间
DEFAULT_START = "2023-06-01"
DEFAULT_END = "2026-08-01"

# 默认初始资金（元）
DEFAULT_CASH = 10_000.0

# 默认标的
DEFAULT_CODES = ["600519.SH", "000001.SZ"]   # 多股票入口（harness 生成策略等）的默认观察/交易池
DEFAULT_STOCK = "002009.SZ"                  # 单股票策略的默认标的
